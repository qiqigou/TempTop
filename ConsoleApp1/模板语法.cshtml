{{each _data.usings val key}}
using {{val}};
{{/each}}

namespace {{_data.namespace}}
{
	public class {{_data.class}}
	{
		{{each _data.fields val key}}
		{{if val.key == 0}}
		[Key]
		{{else if val.key > 0}}
		[Key, Column(Order = {{val.key}})]
		{{/if}}
		public {{val.type}} {{val.name}} { get; set; }
		
		{{/each}}
	}
}